{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make",
            "args": [
                ""
            ],
            "options": {
                "cwd": "${workspaceFolder}/code/"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}